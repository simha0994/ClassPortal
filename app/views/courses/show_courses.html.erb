<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th>Course</th>
	 <th>Grade</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  	<% @show_courses.each do |course| %>
  		<tr>
  			<td><%= course.title %></td>
  			<td><%= course.status %></td>
  		</tr>
  	<% end %>
  </tbody>
</table>

<br>
<%= link_to 'BACK', {:controller =>'users', :action => 'current_user_home'} %>